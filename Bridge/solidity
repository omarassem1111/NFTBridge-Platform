pragma solidity ^0.8.0;

contract Bridge {
    event TransferInitiated(address indexed user, uint tokenId);

    function bridgeNFT(uint tokenId) public {
        emit TransferInitiated(msg.sender, tokenId);
    }
}
